// Practica
// Enviar un screenshot utilziando los 4 tipos de for
// descritos abajo

for (i = 1 ; 1 < 2 ; i++)
    // loop corrient

for (a in b)
    // loop 

for (b of c)
    // lopp

while (a ==b) {
    // se ejecuta mientras se cumpla la condicion
}